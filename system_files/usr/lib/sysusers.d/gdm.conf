# System users and groups for GDM with systemd userdb integration
# See sysusers.d(5) for details

# Create gdm group that GDM will use for dynamic user creation
g gdm 42 - -

# Note: gdm user accounts are now created dynamically by GDM via systemd-userdb
# This file only ensures the gdm group exists for permission management